package bj.ekuiseo.api.domain.enums;

/** Compte touche par une ecriture du registre (V26) : le passager qui paie, la plateforme, le conducteur, l agregateur de paiement. */
public enum LedgerAccount {
    PASSENGER,
    PLATFORM,
    DRIVER,
    PROVIDER
}
